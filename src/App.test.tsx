import{fireEvent,render,screen}from'@testing-library/react';import{beforeEach,expect,it,vi}from'vitest';import App from'./App';import{newRun}from'./engine';
beforeEach(()=>{localStorage.clear();vi.restoreAllMocks()});
it('starts at menu',()=>{render(<App/>);expect(screen.getByRole('button',{name:/start new run/i})).toBeInTheDocument();expect(screen.queryByRole('button',{name:/continue run/i})).not.toBeInTheDocument()});
it('offers continue without automatically resuming a valid save',()=>{localStorage.setItem('ante-up-dice.save.v1',JSON.stringify(newRun(42)));render(<App/>);expect(screen.getByRole('button',{name:/continue run/i})).toBeInTheDocument();expect(screen.queryByRole('button',{name:/roll dice/i})).not.toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:/continue run/i}));expect(screen.getByRole('button',{name:/roll dice/i})).toBeInTheDocument()});
it('confirms before replacing a saved run',()=>{localStorage.setItem('ante-up-dice.save.v1',JSON.stringify(newRun(42)));const confirm=vi.spyOn(window,'confirm').mockReturnValue(false);render(<App/>);fireEvent.click(screen.getByRole('button',{name:/start new run/i}));expect(confirm).toHaveBeenCalledOnce();expect(screen.getByRole('button',{name:/continue run/i})).toBeInTheDocument();expect(JSON.parse(localStorage.getItem('ante-up-dice.save.v1')??'{}').seed).toBe(42)});
it('discards an invalid saved object',()=>{localStorage.setItem('ante-up-dice.save.v1',JSON.stringify({...newRun(42),dice:[9]}));render(<App/>);expect(screen.queryByRole('button',{name:/continue run/i})).not.toBeInTheDocument();expect(localStorage.getItem('ante-up-dice.save.v1')).toBeNull()});
